# =============================================================================
# Authentication (Zitadel OIDC)
# =============================================================================
# Local Development (docker-compose up)
AUTH_ZITADEL_ISSUER=http://localhost:8085

# Get these from Zitadel Console after running: ./scripts/setup-zitadel.sh
# Or manually create an OIDC Web Application in Zitadel Console
AUTH_ZITADEL_CLIENT_ID=your-client-id-from-zitadel
AUTH_ZITADEL_CLIENT_SECRET=your-client-secret-from-zitadel

# Production (uncomment and update for production)
# AUTH_ZITADEL_ISSUER=https://auth.choicesherpa.com
# AUTH_ZITADEL_CLIENT_ID=choice-sherpa-frontend
# AUTH_ZITADEL_CLIENT_SECRET=your-production-secret

# =============================================================================
# Auth.js Configuration
# =============================================================================
# Generate with: openssl rand -base64 32
AUTH_SECRET=your-random-secret-at-least-32-characters

# Trust localhost during development (disable in production)
AUTH_TRUST_HOST=true

# =============================================================================
# API Backend
# =============================================================================
PUBLIC_API_URL=http://localhost:8080

# =============================================================================
# Development Notes
# =============================================================================
# 1. Start infrastructure: docker-compose up -d
# 2. Run setup script: ./scripts/setup-zitadel.sh
# 3. Copy Client ID and Secret from script output or Zitadel Console
# 4. Access Zitadel Console: http://localhost:8085/ui/console
#    Admin credentials: admin / Admin123!
